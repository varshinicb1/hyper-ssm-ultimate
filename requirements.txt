# Hyper-SSM: Lorentzian State-Space Models with Liquid Mixture-of-Experts
# Core dependencies (as of May 2026 research alignment)

torch>=2.4.0
transformers>=4.40.0
numpy>=1.24.0
tqdm>=4.66.0

# Optional but recommended for training
datasets>=2.18.0
accelerate>=0.30.0

# For extreme scaling experiments (train_extreme.py)
# bitsandbytes>=0.43.0   # Uncomment for 8-bit AdamW

# For vision / audio multimodal training
# torchvision>=0.19.0
# torchaudio>=2.4.0

# Development / analysis
matplotlib>=3.8.0
seaborn>=0.13.0

# Note: The custom CUDA extension (hyper_ssm_cuda) must be built separately
# using: python compile_cuda.py build_ext --inplace
# It is already pre-compiled in this repo as hyper_ssm_cuda.cp313-win_amd64.pyd
